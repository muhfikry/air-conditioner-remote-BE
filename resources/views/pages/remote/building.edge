@layout('layouts/app')
@set('title', 'Remote')
@set('subTitle', null)

@section('content')
<div id="rooms-list">

  <div class="row px-lg-20">
    <div class="col-12 mb-6">       
      <div class="card card-flush h-xl-100">

        <div class="px-9 pt-10 pb-2">
          <div class="d-flex flex-column flex-md-row gap-2 align-items-start align-items-md-center justify-content-between">
            <div class="d-flex gap-2 align-items-center w-100">
              <div class="position-relative w-100 mt-1 mt-md-0">
                <i class="ki-outline ki-magnifier fs-2 position-absolute top-50 translate-middle-y ms-4"></i>
                <input type="text" class="search form-control form-control-solid ps-12" placeholder="Search">
              </div>
            </div>
          
            <div class="d-flex gap-2 w-100 w-md-auto mt-2 mt-md-0">
              @if (auth.user.role == 'superadmin' )
              <button data-bs-toggle="modal" data-bs-target="#add" class="btn btn-dark w-100 w-md-auto d-flex align-items-center justify-content-center">
                <i class="ki-duotone ki-plus fs-2 me-2"></i>
                <span>Add</span>
              </button>
              <div class="modal fade" tabindex="-1" id="add">
                <div class="modal-dialog modal-dialog-centered">
                    <form method="POST" action="{{ route('remote.building.store') }}" class="modal-content" id="add">
                        <div class="modal-header">
                            <h3 class="modal-title">Add New Building</h3>
                            <div class="btn btn-icon btn-sm btn-active-light-dark ms-2" data-bs-dismiss="modal" aria-label="Close">
                                <i class="ki-duotone ki-cross fs-1"><span class="path1"></span><span class="path2"></span></i>
                            </div>
                        </div>
                        <div class="modal-body">
                          <div class="mb-3">
                            <label for="exampleFormControlInput1" class="required form-label">Name</label>
                            <input type="text" name="name" class="form-control form-control-solid {{ flashMessages.get('errors.name')? 'is-invalid' : '' }}"  value="{{ flashMessages.get('name', '') ?? '' }}" placeholder="Building Name" required/>
                              <div class="invalid-feedback">
                                {{ flashMessages.get('errors.name') ?? '' }}
                              </div>
                          </div>
                          <div class="mb-3">
                            <label for="exampleFormControlInput1" class="form-label">Description</label>
                            <textarea name="description" class="form-control form-control-solid {{ flashMessages.get('errors.description')? 'is-invalid' : '' }}" rows="4" placeholder="type a description...">{{ flashMessages.get('description', '') ?? '' }}</textarea>
                            <div class="invalid-feedback">
                              {{ flashMessages.get('errors.description') ?? '' }}
                            </div>
                          </div>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-light" data-bs-dismiss="modal">Close</button>
                            <button id="add_submit" class="btn btn-dark me-2 flex-shrink-0">
                              <span class="indicator-label" data-kt-translate="sign-in-submit">Save</span>
                              <span class="indicator-progress">
                                <span data-kt-translate="general-progress">Please wait...</span>
                                <span class="spinner-border spinner-border-sm align-middle ms-2"></span>
                              </span>
                            </button>
                        </div>
                    </form>
                </div>
              </div>
              @endif
            </div>
          </div>
        </div>

          <div class="card-body d-flex align-items-end">  
            <div class="list w-100">                
              @each((item) in data)
                <div>
                  <div class="d-flex align-items-md-center py-2">                    
                    <div class="symbol symbol-40px me-5">
                        <span class="symbol-label">  
                          <i class="ki-duotone ki-bank fs-1">
                            <span class="path1"></span>
                            <span class="path2"></span>
                          </i>                          
                        </span>                
                    </div>
                    <div class="d-md-flex align-items-center flex-stack flex-wrap d-grid gap-1 flex-row-fluid">
                      <div class="me-5">
                        <a href="{{ route('remote.room', { idBuilding:item.id }) }}"class="text-gray-800 fw-bold text-hover-dark fs-5 name">{{ item.name }}</a>
                        <a href="{{ route('remote.room', { idBuilding:item.id }) }}" class="text-gray-500 fw-semibold fs-6 d-block text-start ps-0 description">{{ item.description ?? '-' }}</a>   
                      </div>
                      <div class="d-flex align-items-center mt-5 mt-md-0"> 
                        <span class="badge badge-light-success mx-1">
                          <span class="badge badge-success badge-circle w-10px h-10px me-2"></span> 
                          <span id="active-{{ item.id }}">0</span>
                        </span>
                        <span class="badge badge-light-danger mx-1">
                          <span class="badge badge-danger badge-circle w-10px h-10px me-2"></span> 
                          <span id="inactive-{{ item.id }}">0</span>
                        </span>    
                      </div>
                    </div>
                    @if (auth.user.role == 'superadmin' )
                      <div class="text-end">
                        <a href="#" class="btn btn-icon btn-color-gray-500 btn-active-color-dark justify-content-end" data-kt-menu-trigger="click" data-kt-menu-placement="bottom-end">
                          <i class="ki-outline ki-dots-square fs-1"></i>
                        </a>
                        <div class="menu menu-sub menu-sub-dropdown menu-column menu-rounded menu-gray-600 menu-state-bg fw-semibold fs-7 w-125px py-4" data-kt-menu="true">
                          <div class="menu-item px-3">
                            <a href="#" data-bs-toggle="modal" data-bs-target="#edit{{ item.id }}" class="menu-link px-3">Edit</a>
                          </div>
                          <div class="menu-item px-3">
                            <a id="{{ route('remote.building.destroy', {id: item.id}) }}" class="menu-link px-3 btn-del">Hapus</a>
                          </div>
                        </div>
                      </div>
                      
                      <div class="modal fade" tabindex="-1" id="edit{{ item.id }}">
                        <div class="modal-dialog modal-dialog-centered">
                            <form method="POST" action="{{ route('remote.building.update', { id: item.id }) }}" class="modal-content" id="update{{ item.id }}">
                                <div class="modal-header">
                                    <h3 class="modal-title">Edit Building</h3>
                                    <div class="btn btn-icon btn-sm btn-active-light-dark ms-2" data-bs-dismiss="modal" aria-label="Close">
                                        <i class="ki-duotone ki-cross fs-1"><span class="path1"></span><span class="path2"></span></i>
                                    </div>
                                </div>
                                <div class="modal-body">
                                  <div class="mb-3">
                                    <label for="exampleFormControlInput1" class="required form-label">Name</label>
                                    <input type="text" name="name" class="form-control form-control-solid {{ flashMessages.get('errors.name') ? 'is-invalid' : '' }}"  value="{{ item.name }}" placeholder="Room Name" required/>
                                      <div class="invalid-feedback">
                                        {{ flashMessages.get('errors.name') ?? '' }}
                                      </div>
                                  </div>
                                  <div class="mb-3">
                                    <label for="exampleFormControlInput1" class="form-label">Description</label>
                                    <textarea name="description" class="form-control form-control-solid {{ flashMessages.get('errors.description') ? 'is-invalid' : '' }}" rows="4" placeholder="Type a description...">{{ item.description ?? '' }}</textarea>
                                    <div class="invalid-feedback">
                                      {{ flashMessages.get('errors.description') ?? '' }}
                                    </div>
                                  </div>
                                </div>
                                <div class="modal-footer">
                                    <div class="d-flex flex-stack">
                                      <button type="button" class="btn btn-light" data-bs-dismiss="modal">Close</button>
                                      <button id="update_submit_{{ item.id }}" class="btn btn-dark me-2 flex-shrink-0">
                                        <span class="indicator-label">Save</span>
                                        <span class="indicator-progress">
                                          Please wait...
                                          <span class="spinner-border spinner-border-sm align-middle ms-2"></span>
                                        </span>
                                      </button>
                                    </div>
                                </div>
                            </form>
                        </div>
                      </div>
                    @endif  
                  </div>      
                  <div class="separator separator-dashed my-3"></div> 
                </div>
                @endeach  
            </div>
          </div>
      </div>
    </div>
  </div>

</div>
<script>
  document.getElementById('add').addEventListener('submit', function() {
    var submitButton = document.getElementById('add_submit');
    submitButton.querySelector('.indicator-label').style.display = 'none';
    submitButton.querySelector('.indicator-progress').style.display = 'inline-block';
    submitButton.setAttribute('disabled', 'disabled');
  });
  @each((item) in data)
  document.getElementById('update{{ item.id }}').addEventListener('submit', function() {
    var submitButton = document.getElementById('update_submit_{{ item.id }}');
    submitButton.querySelector('.indicator-label').style.display = 'none';
    submitButton.querySelector('.indicator-progress').style.display = 'inline-block';
    submitButton.setAttribute('disabled', 'disabled');
  });
  @endeach
</script>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/list.js/2.3.1/list.min.js"></script>
<script>
  $(document).ready(function() {
      function updateData() {
          var url = "/remote/stat";

          $.ajax({
              url: url,
              method: 'GET',
              success: function(response) {
                var dataArray = response.data;
                dataArray.forEach(function(item) {
                      $('#active-' + item.id).text(item.activeCount);
                      $('#inactive-' + item.id).text(item.inactiveCount);
                  });
              },
              error: function(xhr) {
                  console.error('Error fetching data:', xhr);
              }
          });
      }
      setInterval(updateData, 1000);
      updateData();
    
    var options = {
      valueNames: [ 'name', 'description' ]
    };
    var roomList = new List('rooms-list', options);
  });
</script>
@endsection
